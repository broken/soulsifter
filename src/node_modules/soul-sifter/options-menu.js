import { css, html, LitElement } from "@polymer/lit-element";

import "./icon-button.js";

class OptionsMenu extends LitElement {
  render() {
    return html`
      <icon-button @click=${this.openMenu} icon="more_vert"></icon-button>
      <div id="menu" class="hide" elevation="3">
        <slot></slot>
      </div>
    `;
  }

  openMenu(e) {
    this.shadowRoot.getElementById('menu').classList.toggle('hide');
    this._closeMenu = (e) => {
      this.shadowRoot.getElementById('menu').classList.toggle('hide');
      document.removeEventListener('click', this._closeMenu, true);
    }
    document.addEventListener('click', this._closeMenu, true);
  }

  static get styles() {
    return [
      css`
        :host {
          position: relative;
          height: 33px;
        }
        #btn {
          line-height: inherit;
          vertical-align: top;
        }
        #menu {
          background-color: var(--ss-options-button-menu-background-color);
          box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
          padding: 15px;
          position: absolute;
          top: 6px;
          right: 8px;
          border-radius: 2px;
          display: block;
          opacity: 1;
          transition: opacity 1s;
          z-index: 1;
        }
        #menu.hide {
          display: none;
          opacity: 0;
        }
      `
    ];
  }
}

window.customElements.define('options-menu', OptionsMenu);
